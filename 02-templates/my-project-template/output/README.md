# Project Output

Write generated CSV files and AD script logs here.

Typical files:

```text
ad-example-com-v1.csv
whatif-log.csv
create-log.csv
```

Review the generated OU CSV and `whatif-log.csv` before running the AD creation command without `-WhatIf`.
